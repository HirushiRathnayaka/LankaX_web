spring.application.name=backend
server.port=8082

app.timezone=${APP_TIMEZONE:Asia/Colombo}

spring.datasource.url=jdbc:mysql://localhost:3306/lankax_db?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=${app.timezone}
spring.datasource.username=${DB_USERNAME:root}
spring.datasource.password=${DB_PASSWORD:}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.jdbc.time_zone=${app.timezone}
spring.jpa.open-in-view=false

spring.jackson.time-zone=${app.timezone}

spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${MAIL_USERNAME:lankax.solution@gmail.com}
spring.mail.password=dcfl wrur ftiz oasm
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000

app.mail.notifications.enabled=${MAIL_NOTIFICATIONS_ENABLED:true}
app.mail.notification-to=${MAIL_NOTIFICATION_TO:${spring.mail.username}}
app.mail.from-name=${MAIL_FROM_NAME:LankaX Website}